1.如果在AndroidManifest.xml没有指定Activity，而在app中启动了那个Activity，会导致app崩溃
2.如果在layout中定义了一个textview，而没有指定其layout_width或者layout_height则导致app崩溃


17:20 2015-4-20
多少时间认为连接断开，导致收到断开的通知

0:20 2016/8/2
软件退出后，再次开启后，one称台上点击连接，3个中有一个会连接不上，再次点击后，才能连接，或者在4称台上一个一个的点击
才能连接，可能是服务发现时间太长了，导致超时了。列举服务失败，可以把服务的枚举超时时间变长


蓝牙打印机需要连接II这个，不能连接IIII这个。