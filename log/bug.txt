第一个设备刚刚连接后，马上第二个设备又去连接，是不是连接第二个设备导致第一个设备断开
是否是第一个设备完全连接后才能再连接。


会产生异步事件，这些事件不是请求后产生的，比如断开事件，就是异步事件，本来
在等待某个请求的回应，结果收到了异步的断开事件。导致


设备已经断开了，队列中因为有请求，所以还在投递设备的请求，这样导致设备回调事件没有对应的处理函数。


最好每个地址的请求都存放到一个响应的队列里面，如果发现连接断开，就清空改地址对应的队列。

可以用

